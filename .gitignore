# Secrets — never commit the OpenAI key (see README)
.env
.env.*
!.env.example
*.key

# Cloned story corpus (regenerate with index_corpus.py)
corpus/

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/

# Annotation caches, pilot selection, validation reports.
# Regenerable; the annotation cache also makes runs resumable, which matters
# across a few hundred failure-prone API calls.
artifacts/

# corpus_index.json IS the shipped deliverable and should be committed, but
# only once built from real annotations -- never a --mock run.

# OS / editor
.DS_Store
.idea/
*.swp
